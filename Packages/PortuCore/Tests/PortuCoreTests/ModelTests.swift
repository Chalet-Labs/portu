import Testing
@testable import PortuCore

@Suite("Model Tests")
struct ModelTests {
    // Placeholder — rewritten after all models are defined
    @Test func placeholder() {
        #expect(true)
    }
}
