import SwiftUI
import PortuCore
import PortuUI

/// Placeholder — HoldingRow will be rebuilt in Plan 02 with PositionToken-based views.
struct HoldingRow: View {
    var body: some View {
        EmptyView()
    }
}
